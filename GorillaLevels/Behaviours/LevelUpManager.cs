using UnityEngine;
using UnityEngine.UI;

namespace GorillaLevels.Behaviours
{
    internal class LevelUpManager : MonoBehaviour
    {
        private Text LevelUpNotification;

        private void LevelUp(int level) // not sure if we're gonna use the level integer
        {
            // Like a animation with confetti that pop ups when you level up
        }
    }
}
